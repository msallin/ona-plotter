## .NET
bin/
obj/
*.user
*.suo
*.cache
*.dll
*.exe
*.pdb

## IDE
.vs/
.vscode/
*.swp
*~

## OS
Thumbs.db
.DS_Store

## Build outputs
publish/
deploy/publish/
deploy/staging/

# Dev-only Blazor config overlay. Used to point at a real SignalK
# server (openplotter.local, a boat's box, etc.) without polluting
# the committed default that everyone else inherits.
OnaPlotter/wwwroot/appsettings.Development.json

## Playwright (running from repo root or UiTests)
test-results/
playwright-report/

# Coverage + Stryker artefacts (local dev only).
coverage-report/
coverage.cobertura.xml
StrykerOutput/
